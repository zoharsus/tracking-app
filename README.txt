To install project:

navigate to backend folder - npm install
navigate to root folder - npm install 

To run(2 CLI's):

on 1: npm run start:server
on 2: ng serve -o

Your default browser should load localhost:4200/
